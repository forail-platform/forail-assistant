# Integration overlay for forge-deploy
# Usage: docker compose -f docker-compose.yml -f docker-compose.integration.yml up -d
services:
  forge-assistant:
    networks:
      - default
      - forge

networks:
  forge:
    name: forge-deploy_forge
    external: true
