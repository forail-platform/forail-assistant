# Integration overlay for forail-deploy
# Usage: docker compose -f docker-compose.yml -f docker-compose.integration.yml up -d
services:
  forail-assistant:
    networks:
      - default
      - forail

networks:
  forail:
    name: forail-deploy_forail
    external: true
